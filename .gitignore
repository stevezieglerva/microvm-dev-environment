node_modules/
*.DS_Store
*.bak
/tmp/

# Sanitized deployment evidence is local-only.
artifacts/install/

# SAM build artifacts
.aws-sam/

# Local SAM CLI config — never commit (your AWS profile, personalized
# parameter_overrides, etc.). See samconfig.toml.example for the template.
samconfig.toml
